# ──────────────────────────────────────────────────────────────────────────────
# PyTorch / Whisper note
# ──────────────────────────────────────────────────────────────────────────────
# The correct PyTorch build depends on whether you have an NVIDIA GPU:
#
#   • No GPU (CPU-only):
#       pip install torch --index-url https://download.pytorch.org/whl/cpu
#
#   • NVIDIA GPU (CUDA 12.x):
#       pip install torch --index-url https://download.pytorch.org/whl/cu126
#
#   • Let the app detect and install automatically:
#       python setup_torch.py
#
# This requirements.txt defaults to the safe CPU-only build.
# ──────────────────────────────────────────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu

PyQt6>=6.4.0
torch>=2.0.0+cpu
openai-whisper>=20231117
